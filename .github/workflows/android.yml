# ================================================================================
# Android CI/CD Pipeline
# Version: 10.1.0 - FIXED ERROR HANDLING (2026)
# ================================================================================

name: Android CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read

    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
      # ✅ ОПТИМИЗАЦИЯ: Агрессивное использование памяти для быстрой сборки
      GRADLE_OPTS: >-
        -Xmx5g
        -Xms2g
        -XX:+UseParallelGC
        -XX:MaxMetaspaceSize=1024m
        -XX:+HeapDumpOnOutOfMemoryError
        -XX:HeapDumpPath=/tmp/heap-dump.hprof
        -Dfile.encoding=UTF-8
        -Dorg.gradle.daemon=true
        -Dorg.gradle.parallel=true
        -Dorg.gradle.caching=true
        -Dorg.gradle.configureondemand=true
        -Dorg.gradle.workers.max=4
      # ✅ Kotlin Compiler оптимизация
      JAVA_TOOL_OPTIONS: >-
        -Xmx3g
        -XX:+UseParallelGC
        -XX:MaxMetaspaceSize=768m
    
    steps:
    # ═══════════════════════════════════════════════════════════════════════════
    # CHECKOUT & SETUP (30 сек)
    # ═══════════════════════════════════════════════════════════════════════════
    
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: gradle  # ✅ Встроенный кеш Gradle

    - name: Set up Android SDK
      uses: android-actions/setup-android@v3

    # ═══════════════════════════════════════════════════════════════════════════
    # AGGRESSIVE CACHING (1-2 мин экономии)
    # ═══════════════════════════════════════════════════════════════════════════
    
    - name: Cache Android SDK
      uses: actions/cache@v4
      with:
        path: |
          /usr/local/lib/android/sdk/platform-tools
          /usr/local/lib/android/sdk/platforms
          /usr/local/lib/android/sdk/build-tools
          /usr/local/lib/android/sdk/licenses
        key: android-sdk-36-${{ runner.os }}-v3
        restore-keys: |
          android-sdk-36-${{ runner.os }}-

    - name: Cache Gradle wrapper & dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', 'gradle/libs.versions.toml') }}
        restore-keys: |
          gradle-${{ runner.os }}-

    - name: Cache Kotlin compiler
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches/modules-2/files-2.1/org.jetbrains.kotlin
          ~/.konan
        key: kotlin-${{ runner.os }}-${{ hashFiles('gradle/libs.versions.toml') }}
        restore-keys: |
          kotlin-${{ runner.os }}-

    # ✅ КРИТИЧНО: Кеш build-cache для incremental compilation
    - name: Cache Gradle build-cache
      uses: actions/cache@v4
      with:
        path: |
          build-cache
          .gradle/build-cache
        key: build-cache-${{ runner.os }}-${{ github.sha }}
        restore-keys: |
          build-cache-${{ runner.os }}-

    # ═══════════════════════════════════════════════════════════════════════════
    # SDK INSTALLATION (только если не закешировано)
    # ═══════════════════════════════════════════════════════════════════════════
    
    - name: Install Android SDK packages
      run: |
        if [ ! -d "/usr/local/lib/android/sdk/platforms/android-36" ]; then
          sdkmanager --install \
            "platform-tools" \
            "platforms;android-36" \
            "build-tools;36.0.0"
        else
          echo "SDK already cached, skipping installation"
        fi
    
    # ═══════════════════════════════════════════════════════════════════════════
    # GRADLE SETUP
    # ═══════════════════════════════════════════════════════════════════════════
    
    - name: Make gradlew executable
      run: chmod +x gradlew
    
    - name: Setup Gradle (с оптимизацией)
      uses: gradle/actions/setup-gradle@v4
      with:
        cache-read-only: false  # ✅ Всегда записываем кеш
        cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
        dependency-graph: disabled  # ✅ Отключаем для скорости
        add-job-summary: on-failure
        gradle-home-cache-cleanup: true

    # ✅ НОВОЕ: Создаём CI-оптимизированный gradle.properties
    - name: Create optimized gradle.properties for CI
      run: |
        cat > gradle.properties << 'EOF'
        # ═══════════════════════════════════════════════════════════════════════
        # CI-OPTIMIZED GRADLE CONFIGURATION
        # ═══════════════════════════════════════════════════════════════════════
        
        # JVM Settings (максимум для GitHub Actions: 7GB - 1.5GB система = 5.5GB)
        org.gradle.jvmargs=-Xmx4500m -Xms2g -XX:MaxMetaspaceSize=1024m -XX:+UseParallelGC -Dfile.encoding=UTF-8
        
        # Kotlin Daemon (отдельный процесс)
        kotlin.daemon.jvmargs=-Xmx2500m -XX:MaxMetaspaceSize=768m -XX:+UseParallelGC
        
        # Gradle Performance
        org.gradle.daemon=true
        org.gradle.parallel=true
        org.gradle.caching=true
        org.gradle.configuration-cache=true
        org.gradle.configuration-cache.problems=warn
        org.gradle.workers.max=4
        org.gradle.vfs.watch=false
        
        # Kotlin Compiler
        kotlin.incremental=true
        kotlin.incremental.usePreciseJavaTracking=true
        kotlin.compiler.execution.strategy=daemon
        
        # Android
        android.useAndroidX=true
        android.enableJetifier=false
        android.nonTransitiveRClass=true
        android.nonFinalResIds=true
        android.enableR8.fullMode=true
        
        # Disable unused features
        android.defaults.buildfeatures.aidl=false
        android.defaults.buildfeatures.renderscript=false
        android.defaults.buildfeatures.resvalues=false
        android.defaults.buildfeatures.shaders=false
        android.defaults.buildfeatures.viewbinding=false
        android.defaults.buildfeatures.databinding=false
        
        # KSP
        ksp.incremental=true
        ksp.incremental.log=false
        EOF

    # ═══════════════════════════════════════════════════════════════════════════
    # MAIN BUILD (SINGLE PASS) - ✅ ИСПРАВЛЕНО
    # ═══════════════════════════════════════════════════════════════════════════
    
    - name: Build with Gradle (Single Pass)
      id: build
      run: |
        FULL_LOG="build_full.log"
        ERRORS_FILE="build_errors.txt"
        
        echo "🚀 Starting optimized build..."
        free -h
        
        # ✅ ИЗМЕНЕНО: Убрали continue-on-error, используем set +e
        set +e
        ./gradlew assembleDebug \
          --stacktrace \
          --no-daemon \
          --max-workers=4 \
          --build-cache \
          --configuration-cache \
          --no-watch-fs \
          --info \
          2>&1 | tee "$FULL_LOG"
        
        BUILD_EXIT_CODE=$?
        set -e
        
        echo ""
        echo "📊 Memory after build:"
        free -h
        
        # ═══════════════════════════════════════════════════════════════════════
        # ERROR EXTRACTION
        # ═══════════════════════════════════════════════════════════════════════
        
        {
          echo "==============================================================="
          echo "  BUILD ERRORS - $(date '+%Y-%m-%d %H:%M:%S')"
          echo "  Commit: $(git rev-parse --short HEAD)"
          echo "  Exit code: $BUILD_EXIT_CODE"
          echo "==============================================================="
          echo ""
          echo ">>> KOTLIN COMPILATION ERRORS:"
          grep -E "^e: " "$FULL_LOG" 2>/dev/null || echo "  (none)"
          echo ""
          echo ">>> JAVA ERRORS:"
          grep -E "error: |Error: " "$FULL_LOG" 2>/dev/null | grep -v "^e: " || echo "  (none)"
          echo ""
          echo ">>> UNRESOLVED REFERENCES:"
          grep -i "unresolved reference" "$FULL_LOG" 2>/dev/null || echo "  (none)"
          echo ""
          echo ">>> GRADLE FAILURE:"
          grep -A 20 "^FAILURE:" "$FULL_LOG" 2>/dev/null || echo "  (none)"
          echo ""
          echo ">>> EXCEPTIONS:"
          grep -E "Exception|Error:" "$FULL_LOG" 2>/dev/null | grep -v "HeapDumpOnOutOfMemoryError" | head -30 || echo "  (none)"
          echo ""
          echo ">>> DEPENDENCY ISSUES:"
          grep -iE "could not (resolve|find|determine)" "$FULL_LOG" 2>/dev/null || echo "  (none)"
          echo ""
          echo ">>> FAILED TASKS:"
          grep -E "Task .* FAILED|BUILD FAILED" "$FULL_LOG" 2>/dev/null || echo "  (none)"
          echo ""
          echo ">>> OTHER ERRORS:"
          grep -i "error" "$FULL_LOG" 2>/dev/null | grep -v -E "^e: |HeapDump|errorprone|error: $" | head -20 || echo "  (none)"
          echo ""
          echo "==============================================================="
          echo "  LAST 50 LINES OF BUILD LOG:"
          echo "==============================================================="
          tail -50 "$FULL_LOG"
        } > "$ERRORS_FILE"
        
        ERROR_COUNT=$(grep -cE "^e: |error:|FAILURE:|Exception" "$FULL_LOG" 2>/dev/null || echo "0")
        echo "error_count=$ERROR_COUNT" >> $GITHUB_OUTPUT
        
        # ✅ ИСПРАВЛЕНО: Корректная установка статуса
        if [ $BUILD_EXIT_CODE -eq 0 ]; then
          echo "build_status=success" >> $GITHUB_OUTPUT
          echo "✅ Build succeeded!"
        else
          echo "build_status=failure" >> $GITHUB_OUTPUT
          echo "❌ Build failed with exit code $BUILD_EXIT_CODE and $ERROR_COUNT errors"
          # ✅ НЕ выходим здесь, чтобы артефакты загрузились
        fi

    # ═══════════════════════════════════════════════════════════════════════════
    # ARTIFACTS & NOTIFICATIONS
    # ═══════════════════════════════════════════════════════════════════════════

    # ✅ ИСПРАВЛЕНО: Проверяем существование APK перед загрузкой
    - name: Upload debug APK artifact
      if: steps.build.outputs.build_status == 'success'
      uses: actions/upload-artifact@v4
      with:
        name: debug-apk-${{ github.sha }}
        path: app/build/outputs/apk/debug/*.apk
        if-no-files-found: warn  # ✅ ИЗМЕНЕНО: warn вместо error
        retention-days: 7

    - name: Upload error logs artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: build-logs-${{ github.sha }}
        path: |
          build_errors.txt
          build_full.log
        if-no-files-found: ignore
        retention-days: 7

    - name: Get commit info
      if: always()
      id: commit
      run: |
        echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
        echo "date=$(date +'%Y%m%d-%H%M')" >> $GITHUB_OUTPUT

    - name: Send Success Notification to Telegram
      if: steps.build.outputs.build_status == 'success'
      continue-on-error: true
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        COMMIT_MSG: ${{ github.event.pull_request.title || github.event.head_commit.message || github.sha }}
      run: |
        if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
          echo "Telegram secrets not set; skipping notification."
          exit 0
        fi
        
        APK_PATH=$(find app/build/outputs/apk/debug -name "*.apk" 2>/dev/null | head -n 1)
        APK_NAME=$(basename "$APK_PATH" 2>/dev/null || echo "app-debug.apk")
        APK_SIZE=$(du -h "$APK_PATH" 2>/dev/null | cut -f1 || echo "?")
        ARTIFACTS_URL="${RUN_URL}#artifacts"
        
        cat > /tmp/tg_message.txt << EOF
        ✅ <b>APK собран!</b>
        
        📱 <code>${APK_NAME}</code>
        📦 ${APK_SIZE}
        📝 ${COMMIT_MSG:0:50}
        👤 ${{ github.actor }}
        🕐 ${{ steps.commit.outputs.date }}
        
        📥 <a href='${ARTIFACTS_URL}'>Скачать</a> | 🔗 <a href='${RUN_URL}'>Logs</a>
        EOF
        
        curl -sS -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
          -d chat_id="${TELEGRAM_CHAT_ID}" \
          -d parse_mode="HTML" \
          --data-urlencode "text=$(cat /tmp/tg_message.txt)"

    - name: Send Build Failure with Errors to Telegram
      if: steps.build.outputs.build_status == 'failure'
      continue-on-error: true
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
      run: |
        if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
          echo "Telegram secrets not set; skipping notification."
          exit 0
        fi
        
        ERRORS_FILE="build_errors.txt"
        ERROR_COUNT="${{ steps.build.outputs.error_count }}"
        
        CAPTION="❌ Build Failed! Errors: ${ERROR_COUNT:-?} | 👤 ${{ github.actor }} | 🕐 ${{ steps.commit.outputs.date }} | 🔗 ${RUN_URL}"
        
        if [ -f "$ERRORS_FILE" ] && [ -s "$ERRORS_FILE" ]; then
          curl -sS -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
            -F chat_id="${TELEGRAM_CHAT_ID}" \
            -F document=@"${ERRORS_FILE}" \
            -F caption="${CAPTION}"
        else
          curl -sS -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=${CAPTION} (Error log not found)"
        fi

    # ✅ ИСПРАВЛЕНО: Проверка в самом конце
    - name: Check build result
      if: steps.build.outputs.build_status == 'failure'
      run: |
        echo "❌ Build failed - check error logs above"
        exit 1