# ================================================================================
# Android CI/CD Pipeline
# Version: 7.0.1 - FIXED (2026 Standards)
#
# ✅ FIX SERIOUS-7: Java 17 → Java 21
#    build.gradle.kts требует Java 21 (compileOptions, kotlinOptions.jvmTarget)
# ================================================================================

name: Android CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
    
    steps:
    - uses: actions/checkout@v4
    
    # ✅ FIX SERIOUS-7: Java 21 вместо 17
    # build.gradle.kts:
    #   compileOptions { sourceCompatibility = JavaVersion.VERSION_21 }
    #   kotlinOptions { jvmTarget = "21" }
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    # ✅ Android SDK (required for compileSdk / build-tools)
    - name: Set up Android SDK
      uses: android-actions/setup-android@v3

    # ✅ Cache Android SDK components (avoids repeated downloads)
    - name: Cache Android SDK packages
      uses: actions/cache@v4
      with:
        path: |
          /usr/local/lib/android/sdk/platform-tools
          /usr/local/lib/android/sdk/platforms/android-36
          /usr/local/lib/android/sdk/build-tools/36.0.0
          /usr/local/lib/android/sdk/licenses
          /usr/local/lib/android/sdk/cmdline-tools
        key: ${{ runner.os }}-androidsdk-api36-bt3600-v1

    - name: Install Android SDK packages
      run: |
        sdkmanager --install \
          "platform-tools" \
          "platforms;android-36" \
          "build-tools;36.0.0"
    
    # ✅ Права на gradlew
    - name: Make gradlew executable
      run: chmod +x gradlew
    
    # ✅ Setup Gradle
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-home-cache-cleanup: true

    # ✅ Cache local Gradle build cache directory used by settings.gradle.kts
    - name: Cache Gradle build-cache directory
      uses: actions/cache@v4
      with:
        path: build-cache
        key: ${{ runner.os }}-build-cache-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', 'gradle/libs.versions.toml') }}
        restore-keys: |
          ${{ runner.os }}-build-cache-
        
    # ✅ Сборка
    - name: Build with Gradle
      id: build
      run: ./gradlew assembleDebug --stacktrace

    - name: Upload debug APK artifact
      if: steps.build.outcome == 'success'
      uses: actions/upload-artifact@v4
      with:
        name: debug-apk
        path: app/build/outputs/apk/debug/*.apk
        if-no-files-found: error
      
    # ⚠️ ВАЖНО: always() чтобы выполнялось даже при ошибке!
    - name: Get commit info
      if: always()
      id: commit
      run: |
        echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
        echo "date=$(date +'%Y%m%d-%H%M')" >> $GITHUB_OUTPUT
        
    # ✅ Отправка ссылки на APK в Telegram (без отправки файла, чтобы не упираться в лимиты)
    - name: Send Success Notification to Telegram
      if: steps.build.outcome == 'success'
      continue-on-error: true
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        COMMIT_MSG: ${{ github.event.pull_request.title || github.event.head_commit.message || github.sha }}
      run: |
        if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
          echo "Telegram secrets not set; skipping notification."
          exit 0
        fi
        
        APK_PATH=$(find app/build/outputs/apk/debug -name "*.apk" | head -n 1)
        APK_NAME=$(basename "$APK_PATH")
        APK_SIZE=$(du -h "$APK_PATH" | cut -f1)
        ARTIFACTS_URL="${RUN_URL}#artifacts"

        TEXT="✅ <b>APK собран успешно!</b>
        
        📱 Файл: <code>${APK_NAME}</code>
        📦 Размер: ${APK_SIZE}
        📝 Commit/PR: ${COMMIT_MSG}
        👤 Автор: ${{ github.actor }}
        🕐 Время: ${{ steps.commit.outputs.date }}
        #️⃣ SHA: ${{ steps.commit.outputs.short_sha }}
        
        📥 <a href='${ARTIFACTS_URL}'>Скачать APK (Artifacts)</a>
        🔗 <a href='${RUN_URL}'>Открыть run</a>"

        curl -sS -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
          -d chat_id="${TELEGRAM_CHAT_ID}" \
          -d parse_mode="HTML" \
          --data-urlencode "text=${TEXT}"
    
    # ❌ Уведомление об ошибке сборки
    - name: Send Build Failure Notification to Telegram
      if: steps.build.outcome == 'failure'
      continue-on-error: true
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        COMMIT_MSG: ${{ github.event.pull_request.title || github.event.head_commit.message || github.sha }}
      run: |
        if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
          echo "Telegram secrets not set; skipping notification."
          exit 0
        fi
        
        curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
             -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
             -d parse_mode="HTML" \
             -d text="❌ <b>Сборка упала!</b>
        
        📝 Commit/PR: ${COMMIT_MSG}
        👤 Автор: ${{ github.actor }}
        🕐 Время: ${{ steps.commit.outputs.date }}
        #️⃣ SHA: ${{ steps.commit.outputs.short_sha }}
        
        🔗 <a href='${RUN_URL}'>Посмотреть логи</a>"