name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: gradle

    - name: Cache Gradle wrapper & dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Cache Android SDK
      uses: actions/cache@v4
      with:
        path: |
          ~/.android/build-cache
          ~/.android/cache
        key: ${{ runner.os }}-android-${{ hashFiles('**/*.gradle*') }}
        restore-keys: |
          ${{ runner.os }}-android-

    - name: Cache Kotlin compiler
      uses: actions/cache@v4
      with:
        path: |
          ~/.konan
          ~/.kotlin
        key: ${{ runner.os }}-kotlin-${{ hashFiles('**/*.gradle*') }}
        restore-keys: |
          ${{ runner.os }}-kotlin-

    - name: Setup Gradle (с оптимизацией)
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-home-cache-cleanup: true
        cache-read-only: false
        cache-write-only: false
        add-job-summary: always
        build-scan-publish: true
        build-scan-terms-of-use-url: "https://gradle.com/terms-of-service"
        build-scan-terms-of-use-agree: "yes"

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle (Single Pass)
      id: build
      run: |
        FULL_LOG="build_full.log"
        ERRORS_FILE="build_errors.txt"
        
        echo "🚀 Starting optimized build..."
        echo "📊 Memory before build:"
        free -h
        echo ""
        
        # ✅ Запускаем сборку БЕЗ перенаправления вывода в GITHUB_OUTPUT
        set +e
        ./gradlew assembleDebug \
          --stacktrace \
          --no-daemon \
          --max-workers=4 \
          --build-cache \
          --configuration-cache \
          --no-watch-fs 2>&1 | tee "$FULL_LOG"
        
        BUILD_EXIT_CODE=${PIPESTATUS[0]}
        set -e
        
        echo ""
        echo "📊 Memory after build:"
        free -h
        echo ""
        echo "🔍 Build exit code: $BUILD_EXIT_CODE"
        
        # ═══════════════════════════════════════════════════════════════════════
        # COMPREHENSIVE ERROR EXTRACTION
        # ═══════════════════════════════════════════════════════════════════════
        
        {
          echo "==============================================================="
          echo "  BUILD REPORT - $(date '+%Y-%m-%d %H:%M:%S')"
          echo "  Commit: $(git rev-parse --short HEAD)"
          echo "  Exit code: $BUILD_EXIT_CODE"
          echo "==============================================================="
          echo ""
          
          if grep -q "BUILD SUCCESSFUL" "$FULL_LOG"; then
            echo "✅ BUILD SUCCESSFUL"
          elif grep -q "BUILD FAILED" "$FULL_LOG"; then
            echo "❌ BUILD FAILED"
          else
            echo "⚠️  BUILD STATUS UNKNOWN (exit code: $BUILD_EXIT_CODE)"
          fi
          
          echo ""
          echo ">>> KOTLIN COMPILATION ERRORS:"
          grep -E "^e: " "$FULL_LOG" 2>/dev/null | head -50 || echo "  (none)"
          
          echo ""
          echo ">>> JAVA COMPILATION ERRORS:"
          grep -E "^.*\.java:[0-9]+: error:" "$FULL_LOG" 2>/dev/null | head -50 || echo "  (none)"
          
          echo ""
          echo ">>> UNRESOLVED REFERENCES:"
          grep -i "unresolved reference" "$FULL_LOG" 2>/dev/null | head -30 || echo "  (none)"
          
          echo ""
          echo ">>> GRADLE FAILURE DETAILS:"
          grep -A 30 "^FAILURE:" "$FULL_LOG" 2>/dev/null || echo "  (none)"
          
          echo ""
          echo ">>> TASK FAILURES:"
          grep -E "> Task .* FAILED" "$FULL_LOG" 2>/dev/null || echo "  (none)"
          
          echo ""
          echo ">>> EXCEPTIONS & STACK TRACES:"
          grep -B 2 -A 10 -E "Exception|at [a-z]+\.[a-z]+\." "$FULL_LOG" 2>/dev/null | grep -v "HeapDumpOnOutOfMemoryError" | head -100 || echo "  (none)"
          
          echo ""
          echo ">>> DEPENDENCY RESOLUTION ERRORS:"
          grep -iE "could not (resolve|find|determine)|dependency.*failed" "$FULL_LOG" 2>/dev/null | head -30 || echo "  (none)"
          
          echo ""
          echo ">>> MANIFEST ERRORS:"
          grep -iE "manifest|androidmanifest" "$FULL_LOG" 2>/dev/null | grep -i error | head -20 || echo "  (none)"
          
          echo ""
          echo ">>> RESOURCE ERRORS:"
          grep -E "AAPT.*error|resource.*error" "$FULL_LOG" 2>/dev/null | head -30 || echo "  (none)"
          
          echo ""
          echo "==============================================================="
          echo "  LAST 100 LINES OF BUILD LOG:"
          echo "==============================================================="
          tail -100 "$FULL_LOG"
          
        } > "$ERRORS_FILE"
        
        # Подсчёт ошибок
        KOTLIN_ERRORS=$(grep -cE "^e: " "$FULL_LOG" 2>/dev/null || echo "0")
        JAVA_ERRORS=$(grep -cE "^.*\.java:[0-9]+: error:" "$FULL_LOG" 2>/dev/null || echo "0")
        TASK_FAILURES=$(grep -cE "> Task .* FAILED" "$FULL_LOG" 2>/dev/null || echo "0")
        TOTAL_ERRORS=$((KOTLIN_ERRORS + JAVA_ERRORS + TASK_FAILURES))
        
        # ✅ Сохраняем переменные БЕЗОПАСНО (без спецсимволов)
        echo "error_count=$TOTAL_ERRORS" >> $GITHUB_OUTPUT
        echo "kotlin_errors=$KOTLIN_ERRORS" >> $GITHUB_OUTPUT
        echo "java_errors=$JAVA_ERRORS" >> $GITHUB_OUTPUT
        echo "task_failures=$TASK_FAILURES" >> $GITHUB_OUTPUT
        
        # ✅ Определяем статус сборки
        if [ $BUILD_EXIT_CODE -eq 0 ] && grep -q "BUILD SUCCESSFUL" "$FULL_LOG"; then
          echo "build_status=success" >> $GITHUB_OUTPUT
          echo ""
          echo "✅ BUILD SUCCESSFUL!"
        else
          echo "build_status=failure" >> $GITHUB_OUTPUT
          echo ""
          echo "❌ BUILD FAILED!"
          echo "   Exit code: $BUILD_EXIT_CODE"
          echo "   Kotlin errors: $KOTLIN_ERRORS"
          echo "   Java errors: $JAVA_ERRORS"
          echo "   Failed tasks: $TASK_FAILURES"
        fi

    - name: Diagnose APK location
      if: always()
      run: |
        echo "🔍 Searching for all APK files..."
        find . -name "*.apk" -type f -exec ls -lh {} \; || echo "No APK files found"
        
        echo ""
        echo "📂 Checking standard debug APK directory..."
        ls -lah app/build/outputs/apk/debug/ 2>/dev/null || echo "Directory app/build/outputs/apk/debug/ not found"
        
        echo ""
        echo "📂 Searching for all 'outputs' directories..."
        find . -type d -name "outputs" -exec ls -la {} \; 2>/dev/null || echo "No outputs directories found"

    - name: Upload debug APK artifact
      if: steps.build.outputs.build_status == 'success'
      uses: actions/upload-artifact@v4
      with:
        name: app-debug-${{ github.sha }}
        path: |
          app/build/outputs/apk/debug/*.apk
          **/build/outputs/apk/debug/*.apk
        if-no-files-found: warn
        retention-days: 14

    - name: Upload ALL logs (full + errors)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: build-logs-${{ github.sha }}
        path: |
          build_full.log
          build_errors.txt
        retention-days: 7

    - name: Get commit info
      if: always()
      id: commit
      run: |
        COMMIT_MSG=$(git log -1 --pretty=format:"%s" | head -c 200)
        COMMIT_AUTHOR=$(git log -1 --pretty=format:"%an")
        echo "message=$COMMIT_MSG" >> $GITHUB_OUTPUT
        echo "author=$COMMIT_AUTHOR" >> $GITHUB_OUTPUT

    - name: Send Success Notification to Telegram
      if: steps.build.outputs.build_status == 'success' && github.event_name == 'push'
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        APK_PATH=$(find app/build/outputs/apk/debug -name "*.apk" -type f | head -1)
        
        if [ -n "$APK_PATH" ]; then
          APK_NAME=$(basename "$APK_PATH")
          APK_SIZE=$(du -h "$APK_PATH" | cut -f1)
          
          MESSAGE="✅ <b>Build Successful!</b>%0A%0A"
          MESSAGE+="📱 <b>APK:</b> ${APK_NAME}%0A"
          MESSAGE+="📦 <b>Size:</b> ${APK_SIZE}%0A"
          MESSAGE+="👤 <b>Author:</b> ${{ steps.commit.outputs.author }}%0A"
          MESSAGE+="💬 <b>Commit:</b> ${{ steps.commit.outputs.message }}%0A"
          MESSAGE+="🔗 <b>SHA:</b> <code>${GITHUB_SHA:0:7}</code>%0A%0A"
          MESSAGE+="📥 <a href=\"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}\">Download APK</a>"
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="${MESSAGE}" \
            -d parse_mode="HTML" \
            -d disable_web_page_preview=true
        else
          echo "⚠️ APK file not found despite successful build!"
        fi

    - name: Send Build Failure with Errors to Telegram
      if: steps.build.outputs.build_status == 'failure' && github.event_name == 'push'
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        MESSAGE="❌ <b>Build Failed!</b>%0A%0A"
        MESSAGE+="👤 <b>Author:</b> ${{ steps.commit.outputs.author }}%0A"
        MESSAGE+="💬 <b>Commit:</b> ${{ steps.commit.outputs.message }}%0A"
        MESSAGE+="🔗 <b>SHA:</b> <code>${GITHUB_SHA:0:7}</code>%0A%0A"
        MESSAGE+="🐛 <b>Errors Found:</b>%0A"
        MESSAGE+="   • Kotlin: ${{ steps.build.outputs.kotlin_errors }}%0A"
        MESSAGE+="   • Java: ${{ steps.build.outputs.java_errors }}%0A"
        MESSAGE+="   • Tasks: ${{ steps.build.outputs.task_failures }}%0A%0A"
        MESSAGE+="📋 <a href=\"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}\">View Details</a>"
        
        curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
          -d chat_id="${TELEGRAM_CHAT_ID}" \
          -d text="${MESSAGE}" \
          -d parse_mode="HTML" \
          -d disable_web_page_preview=true
        
        if [ -f "build_errors.txt" ]; then
          ERROR_SUMMARY="K:${{ steps.build.outputs.kotlin_errors }} J:${{ steps.build.outputs.java_errors }} T:${{ steps.build.outputs.task_failures }}"
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
            -F chat_id="${TELEGRAM_CHAT_ID}" \
            -F document=@build_errors.txt \
            -F caption="📄 Build Error Report (${ERROR_SUMMARY})"
        fi

    - name: Fail job if build failed
      if: steps.build.outputs.build_status == 'failure'
      run: |
        echo "::error::Build failed with ${{ steps.build.outputs.error_count }} errors"
        exit 1