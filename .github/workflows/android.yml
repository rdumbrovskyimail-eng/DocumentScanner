name: Android CI

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    # ✅ ДОБАВЛЕНО: Кэширование Gradle для ускорения
    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
          ~/.gradle/build-cache
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
    
    # ✅ ДОБАВЛЕНО: Кэширование зависимостей Kotlin
    - name: Cache Kotlin Native
      uses: actions/cache@v4
      with:
        path: ~/.konan
        key: ${{ runner.os }}-konan-${{ hashFiles('**/*.gradle*') }}
        restore-keys: |
          ${{ runner.os }}-konan-
    
    # ✅ ИСПРАВЛЕНО: Даём права на выполнение gradlew
    - name: Make gradlew executable
      run: chmod +x gradlew
    
    # ✅ ИСПОЛЬЗУЕМ официальный setup-gradle
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
        
    # ✅ Очистка build директорий
    - name: Clean build directories
      run: ./gradlew clean
        
    # ✅ ОПТИМИЗИРОВАНО: Сборка с флагами производительности
    - name: Build with Gradle
      run: |
        ./gradlew assembleDebug \
          --stacktrace \
          --max-workers=4
      
    - name: Get commit info
      id: commit
      run: |
        echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
        echo "date=$(date +'%Y%m%d-%H%M')" >> $GITHUB_OUTPUT
        
    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: build-${{ steps.commit.outputs.date }}
        name: Build ${{ steps.commit.outputs.short_sha }}
        body: |
          🤖 Автоматическая сборка
          📱 Commit: ${{ github.event.head_commit.message }}
          👤 Автор: ${{ github.actor }}
          🕐 Время: ${{ steps.commit.outputs.date }}
        files: app/build/outputs/apk/debug/*.apk
        draft: false
        prerelease: true
