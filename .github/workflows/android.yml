# ================================================================================
# Android CI/CD Pipeline
# Version: 9.0.0 - WITH ERROR CAPTURE & TELEGRAM FILE UPLOAD
#
# ✅ FIX: gradle/actions/setup-gradle@v4 (fixes removeUnusedEntriesOlderThan error)
# ✅ SPEED: Parallel builds, configuration cache, build cache
# ✅ NEW: Full error capture → Telegram as file
# ================================================================================

name: Android CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

# Cancel in-progress runs for same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
      GRADLE_OPTS: >-
        -Dorg.gradle.daemon=true
        -Dorg.gradle.parallel=true
        -Dorg.gradle.caching=true
        -Dorg.gradle.configureondemand=true
      JAVA_TOOL_OPTIONS: >-
        -Xmx4g
        -Xms512m
        -XX:+UseParallelGC
        -XX:+HeapDumpOnOutOfMemoryError
        -Dfile.encoding=UTF-8
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Set up Android SDK
      uses: android-actions/setup-android@v3

    - name: Cache Android SDK packages
      uses: actions/cache@v4
      with:
        path: |
          /usr/local/lib/android/sdk/platform-tools
          /usr/local/lib/android/sdk/platforms/android-36
          /usr/local/lib/android/sdk/build-tools/36.0.0
          /usr/local/lib/android/sdk/licenses
          /usr/local/lib/android/sdk/cmdline-tools
        key: ${{ runner.os }}-androidsdk-api36-bt3600-v2

    - name: Install Android SDK packages
      run: |
        sdkmanager --install \
          "platform-tools" \
          "platforms;android-36" \
          "build-tools;36.0.0"
    
    - name: Make gradlew executable
      run: chmod +x gradlew
    
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        cache-read-only: ${{ github.ref != 'refs/heads/main' }}
        cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
        dependency-graph: disabled
        add-job-summary: on-failure

    - name: Cache Gradle build-cache directory
      uses: actions/cache@v4
      with:
        path: |
          build-cache
          ~/.gradle/caches/build-cache-1
        key: ${{ runner.os }}-build-cache-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', 'gradle/libs.versions.toml') }}
        restore-keys: |
          ${{ runner.os }}-build-cache-

    # ============================================================================
    # ✅ BUILD WITH FULL ERROR CAPTURE
    # ============================================================================
    - name: Build with Gradle
      id: build
      run: |
        # Создаём файл для полного лога
        FULL_LOG="build_full.log"
        ERRORS_FILE="build_errors.txt"
        
        # Запускаем сборку и сохраняем ВСЁ в лог
        ./gradlew assembleDebug \
          --stacktrace \
          --parallel \
          --build-cache \
          --configuration-cache \
          --no-watch-fs \
          -Dorg.gradle.jvmargs="-Xmx4g -Xms512m -XX:+UseParallelGC -XX:MaxMetaspaceSize=512m" \
          -Dkotlin.daemon.jvm.options="-Xmx2g -XX:+UseParallelGC" \
          -Dkotlin.incremental=true \
          -Dkotlin.compiler.execution.strategy=in-process \
          2>&1 | tee "$FULL_LOG" || BUILD_FAILED=true
        
        # ============================================================================
        # ИЗВЛЕКАЕМ ВСЕ ОШИБКИ (100% coverage)
        # ============================================================================
        {
          echo "═══════════════════════════════════════════════════════════════"
          echo "  BUILD ERRORS - $(date '+%Y-%m-%d %H:%M:%S')"
          echo "  Commit: $(git rev-parse --short HEAD)"
          echo "═══════════════════════════════════════════════════════════════"
          echo ""
          
          # 1. Kotlin ошибки компиляции (e: file.kt:line: error)
          echo "▶ KOTLIN COMPILATION ERRORS:"
          grep -E "^e: " "$FULL_LOG" || echo "  (none)"
          echo ""
          
          # 2. Java ошибки
          echo "▶ JAVA ERRORS:"
          grep -E "error: |Error: " "$FULL_LOG" | grep -v "^e: " || echo "  (none)"
          echo ""
          
          # 3. Unresolved references (очень частая ошибка)
          echo "▶ UNRESOLVED REFERENCES:"
          grep -i "unresolved reference" "$FULL_LOG" || echo "  (none)"
          echo ""
          
          # 4. Gradle FAILURE блок
          echo "▶ GRADLE FAILURE:"
          grep -A 20 "^FAILURE:" "$FULL_LOG" || echo "  (none)"
          echo ""
          
          # 5. Exception stacktraces
          echo "▶ EXCEPTIONS:"
          grep -E "Exception|Error:" "$FULL_LOG" | grep -v "HeapDumpOnOutOfMemoryError" | head -30 || echo "  (none)"
          echo ""
          
          # 6. Could not resolve / find
          echo "▶ DEPENDENCY ISSUES:"
          grep -iE "could not (resolve|find|determine)" "$FULL_LOG" || echo "  (none)"
          echo ""
          
          # 7. Task failures
          echo "▶ FAILED TASKS:"
          grep -E "Task .* FAILED|BUILD FAILED" "$FULL_LOG" || echo "  (none)"
          echo ""
          
          # 8. Любые строки с "error" которые могли пропустить
          echo "▶ OTHER ERRORS (grep 'error'):"
          grep -i "error" "$FULL_LOG" | grep -v -E "^e: |HeapDump|error: $" | head -20 || echo "  (none)"
          echo ""
          
          echo "═══════════════════════════════════════════════════════════════"
          echo "  LAST 50 LINES OF BUILD LOG:"
          echo "═══════════════════════════════════════════════════════════════"
          tail -50 "$FULL_LOG"
          
        } > "$ERRORS_FILE"
        
        # Проверяем, есть ли реальные ошибки
        ERROR_COUNT=$(grep -cE "^e: |error:|FAILURE:|Exception" "$FULL_LOG" || echo "0")
        echo "error_count=$ERROR_COUNT" >> $GITHUB_OUTPUT
        echo "errors_file=$ERRORS_FILE" >> $GITHUB_OUTPUT
        
        # Если сборка упала - выходим с ошибкой (но ПОСЛЕ сохранения логов)
        if [ "$BUILD_FAILED" = "true" ]; then
          echo "build_status=failure" >> $GITHUB_OUTPUT
          exit 1
        else
          echo "build_status=success" >> $GITHUB_OUTPUT
        fi

    # ============================================================================
    # ✅ UPLOAD ARTIFACTS
    # ============================================================================
    - name: Upload debug APK artifact
      if: steps.build.outputs.build_status == 'success'
      uses: actions/upload-artifact@v4
      with:
        name: debug-apk
        path: app/build/outputs/apk/debug/*.apk
        if-no-files-found: error
        retention-days: 7

    - name: Upload error logs artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: build-errors
        path: |
          build_errors.txt
          build_full.log
        if-no-files-found: ignore
        retention-days: 7

    # ============================================================================
    # ✅ COMMIT INFO (always runs)
    # ============================================================================
    - name: Get commit info
      if: always()
      id: commit
      run: |
        echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
        echo "date=$(date +'%Y%m%d-%H%M')" >> $GITHUB_OUTPUT

    # ============================================================================
    # ✅ SUCCESS NOTIFICATION
    # ============================================================================
    - name: Send Success Notification to Telegram
      if: steps.build.outputs.build_status == 'success'
      continue-on-error: true
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        COMMIT_MSG: ${{ github.event.pull_request.title || github.event.head_commit.message || github.sha }}
      run: |
        if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
          echo "Telegram secrets not set; skipping notification."
          exit 0
        fi
        
        APK_PATH=$(find app/build/outputs/apk/debug -name "*.apk" | head -n 1)
        APK_NAME=$(basename "$APK_PATH")
        APK_SIZE=$(du -h "$APK_PATH" | cut -f1)
        ARTIFACTS_URL="${RUN_URL}#artifacts"

        TEXT="✅ <b>APK собран успешно!</b>

📱 Файл: <code>${APK_NAME}</code>
📦 Размер: ${APK_SIZE}
📝 Commit/PR: ${COMMIT_MSG}
👤 Автор: ${{ github.actor }}
🕐 Время: ${{ steps.commit.outputs.date }}
#️⃣ SHA: ${{ steps.commit.outputs.short_sha }}

📥 <a href='${ARTIFACTS_URL}'>Скачать APK (Artifacts)</a>
🔗 <a href='${RUN_URL}'>Открыть run</a>"

        curl -sS -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
          -d chat_id="${TELEGRAM_CHAT_ID}" \
          -d parse_mode="HTML" \
          --data-urlencode "text=${TEXT}"

    # ============================================================================
    # ❌ FAILURE NOTIFICATION - SENDS ERROR FILE TO TELEGRAM
    # ============================================================================
    - name: Send Build Failure with Errors to Telegram
      if: failure() || steps.build.outputs.build_status == 'failure'
      continue-on-error: true
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        COMMIT_MSG: ${{ github.event.pull_request.title || github.event.head_commit.message || github.sha }}
      run: |
        if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
          echo "Telegram secrets not set; skipping notification."
          exit 0
        fi
        
        ERRORS_FILE="build_errors.txt"
        ERROR_COUNT="${{ steps.build.outputs.error_count }}"
        
        # Короткое сообщение (caption для файла)
        CAPTION="❌ <b>Build Failed!</b>

📝 ${COMMIT_MSG}
👤 ${{ github.actor }}
🕐 ${{ steps.commit.outputs.date }}
#️⃣ ${{ steps.commit.outputs.short_sha }}
⚠️ Errors: ${ERROR_COUNT:-?}

🔗 <a href='${RUN_URL}'>Full logs</a>"

        # Отправляем файл с ошибками в Telegram
        if [ -f "$ERRORS_FILE" ] && [ -s "$ERRORS_FILE" ]; then
          # Файл существует и не пустой - отправляем как документ
          curl -sS -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
            -F chat_id="${TELEGRAM_CHAT_ID}" \
            -F document=@"${ERRORS_FILE}" \
            -F caption="${CAPTION}" \
            -F parse_mode="HTML"
        else
          # Файла нет - отправляем просто сообщение
          curl -sS -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d parse_mode="HTML" \
            --data-urlencode "text=${CAPTION}

(Error log file not found)"
        fi