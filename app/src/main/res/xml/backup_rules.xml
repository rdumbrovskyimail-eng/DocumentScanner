<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Бэкапим ТОЛЬКО то, что явно указано -->
    <include domain="database" path="." />
    <include domain="file" path="documents/" />
    
    <!-- НЕТ exclude - просто не включаем то, что не нужно -->
    <!-- sharedpref, datastore, cache НЕ включены = НЕ бэкапятся -->
</full-backup-content>